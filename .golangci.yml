version: "2"

linters:
  default: standard
  enable:
    - unconvert
    - unparam
    - misspell
    - gocritic
    - errorlint
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - unparam

formatters:
  enable:
    - gofmt
    - goimports
